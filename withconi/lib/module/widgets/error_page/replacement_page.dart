import '../../../import_basic.dart';

abstract class ReplacementPage extends StatelessWidget {
  const ReplacementPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
